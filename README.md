Typescript + React front end Template
set to use OIDC, FluentUI React-Components, React-Router Data APIs and Tanstack React Query
Uses environment variables for configuration.

New projects replace token __PROJECT_NAME__ with Project Name